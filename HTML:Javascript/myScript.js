function myFunction() {
document.getElementById("demo").innerHTML = "Paragraph"+" "+"changed.";
}
function callAlert() {
  /* I should really learn to comment more
  This is a multiline comment where I discuss the below program, which
  does some math and then outputs the solution */
  var x, y, z; // declaring x y z
  //x=5;
  // some variable statements
  x=8; y=6;
  z=x+y;
  // will not call alert for now
  //window.alert(z)
  document.getElementById("demo").innerHTML = z/5.
  document.getElementById("demo2").innerHTML = z*5.}
