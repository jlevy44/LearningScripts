%when debugging functions, use disp to print messages
disp('starting loop')
disp('loop is over')
%program pauses at breakpoints, can use debugger options during it
%use command window to probe variables
%disp(strcat(['loop iteration ',num2str(n)])); 
%shows when loop was broken, or where at in loop

%use debuggers to fix errors in code... see function plotTenLines

%http://www.mathworks.com/matlabcentral/
%other peoples code to expand functionality of matlab

